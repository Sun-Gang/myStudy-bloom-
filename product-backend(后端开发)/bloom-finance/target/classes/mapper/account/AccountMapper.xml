<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bloomfinance.account.repository.AccountRepository">

    <!-- 查询用户所有账户 -->
    <select id="findByUserId" resultType="com.bloomfinance.account.domain.entity.AccountEntity">
        SELECT id, user_id, account_type, bank_name, card_no, balance, status, created_at, updated_at, deleted
        FROM t_account
        WHERE deleted = 0
        AND user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 根据用户ID和账户类型查询账户 -->
    <select id="findByUserIdAndAccountType" resultType="com.bloomfinance.account.domain.entity.AccountEntity">
        SELECT id, user_id, account_type, bank_name, card_no, balance, status, created_at, updated_at, deleted
        FROM t_account
        WHERE deleted = 0
        AND user_id = #{userId}
        AND account_type = #{accountType}
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID和用户ID查询账户 -->
    <select id="findByIdAndUserId" resultType="com.bloomfinance.account.domain.entity.AccountEntity">
        SELECT id, user_id, account_type, bank_name, card_no, balance, status, created_at, updated_at, deleted
        FROM t_account
        WHERE deleted = 0
        AND id = #{id}
        AND user_id = #{userId}
    </select>

</mapper>