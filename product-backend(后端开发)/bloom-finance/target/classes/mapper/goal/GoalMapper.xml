<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bloomfinance.goal.repository.GoalRepository">

    <!-- 根据用户ID查询目标列表 -->
    <select id="findByUserId" resultType="com.bloomfinance.goal.domain.entity.GoalEntity">
        SELECT * FROM t_goal
        WHERE deleted = 0 AND user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 根据用户ID和状态查询目标 -->
    <select id="findByUserIdAndStatus" resultType="com.bloomfinance.goal.domain.entity.GoalEntity">
        SELECT * FROM t_goal
        WHERE deleted = 0 AND user_id = #{userId} AND status = #{status}
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID和用户ID查询目标 -->
    <select id="findByIdAndUserId" resultType="com.bloomfinance.goal.domain.entity.GoalEntity">
        SELECT * FROM t_goal
        WHERE deleted = 0 AND id = #{id} AND user_id = #{userId}
        LIMIT 1
    </select>

</mapper>
